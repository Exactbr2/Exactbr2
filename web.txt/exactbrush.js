// Select all menu items
const fileInput = document.getElementById('myFile');
const triggerBtn0 = document.querySelector('.labelcent2');

// Open file menu when the image is clicked
triggerBtn0.addEventListener('click', () => {
fileInput.click();
});